# Phase 9 correction gate table

| Gate | Result | Evidence |
|---|---|---|
| 0 | demonstrated | Exact clean baseline, pre-change contract check and 96-test pass retained; local correction branch only. |
| R1 | demonstrated | Complete build uses established resolution/build/runtime planning and blocks unequal selection roots. |
| R2 | demonstrated | Focused fixture builds effective provider, adapter, replacement, exclusion, and re-inclusion choices. |
| R3 | demonstrated | Selected specialist generator emits real scene-integration bytes through the transactional action boundary; reuse and invalidation dimensions are tested. |
| R4 | demonstrated | Generation records activation/runtime roots; semantic override fixture fails with `FPM_GENERATION_SEMANTIC_OVERRIDE_FORBIDDEN`. |
| R5 | demonstrated | G1 export/import into an empty manager reproduces generation, runtime plan, artifact, tick, state, scene, and render roots; corruption fails before import/activation. |
| R6 | demonstrated | Failed G2 creates no G2 session, retains the failed attempt, and reactivates G1 under a distinct rollback session referenced by the active pointer. |
| R7 | demonstrated | Fifteen durable identities are machine checked and indexed across four closed, versioned schema families with owners and identity rules. |
| R8 | demonstrated | Retained end-to-end fixture creates G0/G1/G2 through real complete builds and executes imported G1. |
| R9 | demonstrated | Integrated 10/100/1,000 cases separately report planning, authoritative build, validation, package-only materialisation, and commit; leaf/full-closure and diagnostic locality evidence retained. |
| Regression | demonstrated | `npm run contracts:check` passes; serial full suite passes 100/100. |

The earlier parallel full-suite run is retained as a 99/100 diagnostic receipt. Its sole failure was a fixed two-second portable-handler process timeout under unrelated file-level test contention. Serial file orchestration then passed the same test and all other tests.
